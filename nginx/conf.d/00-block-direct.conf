# Bloqueia acesso direto pelo IP (sem passar pelo Cloudflare)
# Ex: http://66.253.112.233/ cai aqui e não recebe resposta (444 = fecha conexão)
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;
    return 444;
}
